spring:
 datasource:
  # This in-memory database exists only for 2 seconds while the build runs
  url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
  driver-class-name: org.h2.Driver
  username: sa
  password: ""

 jpa:
  database-platform: org.hibernate.dialect.H2Dialect